# 复制为 .env 并修改。所有值均为可选，未设置时使用默认值。
NODE_ENV=development
PORT=3000

# 后台登录密码（必填，用于 /admin 登录）
ADMIN_PASSWORD=change-me-please

# 会话密钥（生产环境必须设置；会话使用内存存储，重启后需重新登录）
SESSION_SECRET=

# 站点公开地址（用于 RSS / sitemap 的绝对链接）
BASE_URL=http://localhost:3000
